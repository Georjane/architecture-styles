<% if Article.all.empty? %>
  <div class="row  logimage">
    <div class="col-md-6">
      <h3>Please create a New Category <%= link_to "here", new_section_path, class: "new_section" %> 
      and add Articles <%= link_to 'here', new_article_path %></h3>
    </div>
    <div class="col-md-6">
      <%= link_to image_tag("wfh_1.svg", alt: "Remote working image") %>
    </div>
  </div>
<% else %>
  <%= render 'homepage' %>
<% end %>
