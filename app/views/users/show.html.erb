<% if Section.all.empty? %>
  <div class="row  logimage">
    <div class="col-md-6">
      <h3>PLease create a New Category <%= link_to "Create Category", new_section_path, class: "new_section" %></h3>
    </div>
    <div class="col-md-6">
      <%= link_to image_tag("wfh_1.svg", alt: "Remote working image") %>
    </div>
  </div>
<% else %>
  <%= render 'homepage' %>
<% end %>
